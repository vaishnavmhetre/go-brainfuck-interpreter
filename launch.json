{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch (program)",
            "type": "go",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "mode": "auto"
            // Add other configuration options here
        }
    ]
}